/**
 * main - program that prints its name
 * @argc: Number of command line arguments
 * @argv: Array name
 * Return: 0.
 */
#include <stdio.h>
#include "main.h"
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
