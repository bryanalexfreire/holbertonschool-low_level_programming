/**
 * main - function-like macro SUM(x, y) that computes the sum..
 * Return: 0.
 */
#include "4-sum.h"
#include "4-sum.h"
#include <stdio.h>
int main(void)
{
	int s;

	s = SUM(98, 1024);
	printf("%d\n", s);
	return (0);
}
