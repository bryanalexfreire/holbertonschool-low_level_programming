/**
 * main - Function that prints the alphabet, in lowercase
 * Return: Always 0 (Succes)
 */
#include <studio.h>
int main(void)
{
	int n;

	for (n = 'a'; n <= 'z'; n++)
	{
		putchar(n);
	}
	putchar('\n');
}
