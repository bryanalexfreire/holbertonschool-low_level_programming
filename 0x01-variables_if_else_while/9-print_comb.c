/**
 * main - Prints an possible combinations of single-digit numbers
 * Return: 0 on success.
 */

#include <stdio.h>

int main(void)
{
	int b = '0';

	while (b <= '0')
	{
		putchar(i);
		if (b != '9')
		{
			putchar(',');
			putchar(' ');
		}
		++b;
	}
	return (0);
}
