/**
 * main - Prints all the numbers of base 16 in lowercase
 * Return: 0 on success.
 */

#include <stdio.h>

int main(void)
{
	int b = '0';

	while (i <= '9')
	{
		putchar(i);
		i++;
	}
	i = 'a';
	while(i <= 'f')
	{
		putchar(i);
		i++;
	}
	return (0);
}
